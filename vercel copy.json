{
  "version": 2,
  "builds": [
    {
      "src": "./index.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./index.mjs"
    }
  ],
  "buildCommand": "npm install --prefix && npm install --prefix client && npm run build --prefix client"
}
